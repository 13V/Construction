{
  "name": "crewline-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Native shell for the Crewline worker app. Exists for one reason: background location.",
  "scripts": {
    "build:web": "cd ../dashboard && VITE_SURFACE=worker npm run build",
    "sync": "npm run build:web && cap sync",
    "android": "npm run sync && cap open android",
    "ios": "npm run sync && cap open ios",
    "run:android": "npm run sync && cap run android",
    "run:ios": "npm run sync && cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor-community/background-geolocation": "^1.2.28"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
