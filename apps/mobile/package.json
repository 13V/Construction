{
  "name": "crewline-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Native shell for the Crewline worker app.",
  "scripts": {
    "build:web": "cd ../dashboard && VITE_API_BASE=\"${VITE_API_BASE:-https://construction-opal-three.vercel.app}\" VITE_SURFACE=worker npm run build",
    "sync": "npm run build:web && cap sync",
    "android": "npm run sync && cap open android",
    "ios": "npm run sync && cap open ios",
    "run:android": "npm run sync && cap run android",
    "run:ios": "npm run sync && cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/keyboard": "^8.0.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "typescript": "^7.0.2"
  }
}
